name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Test import (verify all modules load)
        run: python -c "from src.main import main; print('✅ All modules imported successfully')"

      - name: Build Docker image
        run: docker build -t shipment-management-system:latest .

      - name: Success message
        run: echo "🚀 Docker image built successfully – container works!"
